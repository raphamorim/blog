<section class="tags-list">
    <ul>
        <% @tags.each do |tag| %>
            <li>
                <%= link_to tag.name, tag_path(tag.name)  %>
            </li>
        <% end %>
    </ul>
    <br class="tags-clear-section" />
</section>
