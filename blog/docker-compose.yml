version: '2'
services:
  db:
    container_name: blog_db
    image: postgres
    ports:
      - "5432:5432"
  web:
    container_name: blog_app
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    volumes:
      - .:/blog
    ports:
      - "3000:3000"
    depends_on:
      - db
